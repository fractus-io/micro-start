spring.profiles.active: development
---
spring:
  application:
    name: hello-service
server:
  port: 9090
  cloud:
    config:
      uri: http://localhost:8888
  
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://localhost:8888
---
spring:
  profiles: default
  cloud:
    config:
      uri: http://localhost:8888

